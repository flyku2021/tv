
// 一些初始化的命令

git config --global user.name 'positivesingleslogin'
git config --global user.email 'ps.partner@gmail.com'

git status

git init

git add .

git commit -m "2026-5-20"
git commit -m "pull"

git remote add origin https://codeberg.org/simonren/Television.git

git clone https://codeberg.org/simonren/Television.git

git pull

git pull origin main

git pull origin main --allow-unrelated-histories

git push -u origin main


// 使用代理 proxy: 127.0.0.1:21882

git config --global http.proxy http://127.0.0.1:21882
git config --global https.proxy http://127.0.0.1:21882


>>>>> 批量

git add .
git commit -m "2026-6-5"
git push -u origin main


===========================================

echo "# tv" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/positivesingleslogin/go.git
git push -u origin main


